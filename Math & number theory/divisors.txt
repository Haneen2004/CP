vector<ll> divisors(ll n){
    vector<ll>v; ll i=1;
    for (; i*i <n ; ++i) {
        if(n%i==0){
            v.push_back(i);   v.push_back(n/i);
        }
    }
    if(i*i==n) v.push_back(i);    return v;
}